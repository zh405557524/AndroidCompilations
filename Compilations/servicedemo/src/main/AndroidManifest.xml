<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.soul.servicedemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>

        <service android:name=".service.TextService"/>

        <activity android:name=".MusicPlayerActivity">
        </activity>

        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="true">
        </service>

        <activity android:name=".LockScreenActivity">

        </activity>
        <!--
        <receiver
            android:name=".receiver.ScreenReceiver"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.SCREEN_OFF"/>
                <action android:name="android.intent.action.SCREEN_ON"/>
            </intent-filter>
        </receiver>
        -->
        <service
            android:name=".service.ScrennService"
            android:enabled="true"
            android:exported="true">
        </service>

        <activity android:name=".RemoteServiceActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".service.RemoteService"
            >

            <intent-filter>
                <action android:name="com.soul.servicedemo"/>
            </intent-filter>
        </service>
    </application>

</manifest>